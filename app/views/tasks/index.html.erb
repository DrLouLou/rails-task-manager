<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li ><%= link_to task.title, task_path(task) %></li>
    <p> <%= link_to 'Update new task', task_path(task) %></p>
  <% end %>
</ul>
<p> <%= link_to 'Create new task', 'tasks/new' %></p>
